<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="esaip.studyflow_v1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic__logo_sf"
        android:label="StudyFlow"
        android:roundIcon="@mipmap/ic__logo_sf"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Login" />
        <activity android:name=".Profil" />
        <activity android:name=".Calendar" />
        <activity
            android:name=".Community"
            android:label="" />
        //Remove title from title bar
        <activity android:name=".Storage" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="esaip.studyflow_v1.MainActivity" />
        </activity>
    </application>

</manifest>